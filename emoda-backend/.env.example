// Modelo do módulo .env legado — use APENAS em desenvolvimento local.
// Copie para emoda-backend/.env (o arquivo é ignorado pelo git).
// Em produção nada disso é usado: o config/env.js lê as variáveis de ambiente.
module.exports = {
    authSecret: '',
    db: {
        host: '127.0.0.1',
        port: 3306,
        database: 'emoda',
        user: '',
        password: ''
    }
}
